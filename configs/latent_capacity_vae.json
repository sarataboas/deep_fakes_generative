{
  "experiment_name": "vae_latent256_beta2",
  "seed": 42,

  "data": {
    "metadata_path": "data/metadata.csv",
    "train_source": ["inpainting", "insight", "text2img"]
  },

  "preprocessing": {
    "img_size": 64,
    "type": "vae"
  },

  "model": {
    "latent_dim": 256,
    "feature_maps": 32,
    "img_channels": 3
  },

  "training": {
    "num_epochs": 150,
    "batch_size": 64,
    "num_workers": 2,

    "lr": 0.0002,
    "weight_decay": 0.0,

    "kl_annealing": {
      "enabled": true,
      "type": "linear",
      "start_beta": 0.0,
      "end_beta": 2.0,
      "warmup_epochs": 70
    },

    "scheduler": {
      "type": "cosine",
      "T_max": 150
    },

    "early_stopping": {
      "enabled": false
    },

    "save_dir": "outputs/vae_latent256_beta2",
    "checkpoint_dir": "checkpoints",

    "sample_every": 5,
    "num_generated_samples": 16,
    "num_reconstruction_images": 8
  }
}